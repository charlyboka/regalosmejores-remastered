"""Pipeline implementations.

Every module in this package is imported automatically by the registry, so adding a pipeline is
just dropping a file here with an ``@register``-decorated ``Pipeline`` subclass.
"""
